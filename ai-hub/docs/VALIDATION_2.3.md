# AI Hub 2.3.0 验证记录

验证平台：Windows x64、Python 3.11、本机 Node.js。测试全部使用独立临时资产，未使用真实模型或图库执行整理。

- `python -B -m unittest discover -s tests -v`：120项，115通过、5项因Windows创建符号链接权限不足而跳过。真实NTFS目录联接扫描/源目录替换/目标目录替换测试和文件重解析属性模拟均通过。
- `node --test tests/*.test.js`：33项全部通过。
- `node --check frontend/app.js`、`frontend/navigation.js`、`frontend/organizer.js`：全部通过。
- 实际HTTP临时服务器验证预览→建立硬链接→撤销；原件内容、inode、大小与mtime保持。拒绝异常Host、跨来源请求、超限请求和外部路径，扫描期间更改范围返回冲突。
- 浏览器验证全新安装提示、保存本机安全区、分类预览、建立确认、批次记录、撤销确认与已撤销状态；未知模型使用中文待确认标签，原路径与目标路径均可核对。
- 目录与配置覆盖新旧布局、根目录散落文件、盘符失效、损坏配置保留、原子保存、并发和旧计划；分批整理能跳过已有入口继续后续文件。

桌面外壳代码与EXE沿用已验证版本；本次更新Python后台、前端和发行说明，不改变桌面运行依赖。发行包仅包含白名单程序文件，不包含data、凭据、模型、图库、日志或个人报告。

硬链接共享原件内容，不是备份。未执行全区真实资产迁移；实际无法确认用途的模型保持待确认，应用/训练包等按说明跳过。
